// Since data in this file is not that big,
// we could just move those values into the
// "deterministicPartitionKey" function itself.
// But just for sake of clearness and less commenting
// in the function itself, it'd better to store
// them here and assign self explanatory name to them.

export const TRIVIAL_PARTITION_KEY = "0";
export const MAX_PARTITION_KEY_LENGTH = 256;
